export const routerSelector = x => x.router
export const localeSelector = ({ localeData }) => localeData.locale
export const messagesSelector = ({ localeData }) => localeData.messages
export const contractSelector = x => x.contract
